#!/bin/bash
logInfo "$BASH_SOURCE" "$LINENO" 'Configuring output directory...'

requireVariable "$BASH_SOURCE" "$LINENO" BO_Project || return 1
createDirectory $BO_Project/out                     || return 1

# Return, but do NOT exit, with a success code
return 0

