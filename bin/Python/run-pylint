#!/bin/bash
# Run pylint on this project

echo "run-pylint..."
cd "${BO_Project}/src"

_Cmd=pylint
_Cmd="$_Cmd --rcfile=${BO_Project}/cfg/pylintrc"
_Cmd="$_Cmd ${HomePackage}"
$_Cmd | tee ${BO_Project}/out/pylint.out

