#!/bin/bash
# Use Python Pip to install dependencies from a previous freeze.

# Install from the previously-frozen dependency list
pip install < "${VIRTUAL_ENV}/freeze.pip"

# Report the installed dependencies
pip freeze
