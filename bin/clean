#!/bin/bash
# Clean the project directory of all output and temporary files

rm -fr "${VIRTUAL_ENV}/dist"
rm -r "${VIRTUAL_ENV}/*.pyc"
rm "${VIRTUAL_ENV}/.coverage"
rm "${VIRTUAL_ENV}/MANIFEST"
rm "${VIRTUAL_ENV}/nosetests.xml"
rm "${VIRTUAL_ENV}/pylint.out"
rm "${VIRTUAL_ENV}/sloccount.sc"

